# Build `project` docker
./bin/build.sh

# Start `project` docker
./bin/start.sh
# Accessing notebooks (Note: goto after server started): visit http://localhost:9080/lab
# (OR) Enter into docker bash
./bin/docker_bash.sh

# Restart `project` docker
./bin/restart.sh

# Stop `project` docker
./bin/stop.sh